/**
 * Where people reach us.
 *
 * Kept in one place because it appears on four pages, three of which are
 * policy pages where the address is the whole point: the privacy policy tells
 * readers to use it to exercise their DPDP rights, and the refund policy tells
 * them to use it to raise an exception. All four shipped with an empty
 * mailto:, so every one of those instructions pointed at nothing.
 */
export const SUPPORT_EMAIL = 'admin@distilledcrux.com';
export const SUPPORT_MAILTO = `mailto:${SUPPORT_EMAIL}`;
